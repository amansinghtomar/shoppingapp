import styled from "styled-components";

export const CategoryContainer = styled.div`
   margin: 10px;
   width: 200px;
   display: flex;
   flex-direction: column;
   gap: 5px;

   &:hover {
      cursor: pointer;
   }
`;
